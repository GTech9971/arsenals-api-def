openapi: 3.0.2
info:
  title: "銃取得レスポンス"
  version: "1.0"
paths: {}
components:
  responses:
    OK:
      description: |-
        OK
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/fetchGunResponse"

  schemas:
    fetchGunResponse:
      title: 銃取得レスポンス
      type: object
      allOf:
        - $ref: "../commons/model.yaml#/components/schemas/BaseResponse"
        - type: object
          properties:
            data:
              type: object
              properties:
                gun:
                  $ref: "./model.yaml#/components/schemas/Gun"
              required:
                - gun
